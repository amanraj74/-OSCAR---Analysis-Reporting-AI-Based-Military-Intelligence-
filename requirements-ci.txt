# Slim requirements for CI runners (GitHub Actions ubuntu-latest + windows-latest).
#
# Why slim:
#   - Torch + Transformers + Sentence-Transformers + BERTopic + XGBoost + MLflow + Prophet
#     add ~2 GB and 4-6 minutes of install time. CI doesn't need them.
#   - All our ML/NLP modules have a `force_mode` fallback so tests work without them.
#   - Same fallback chain used by Streamlit Cloud and CI environments.
#
# Result:
#   - Install: ~2 min instead of ~5 min
#   - Tests: still 100% passing (because of force_mode)
#   - Lint: still works

# Core runtime
python-dotenv>=1.0.1,<2.0.0
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.4.0,<3.0.0
PyYAML>=6.0.2,<7.0.0
loguru>=0.7.2,<1.0.0

# HTTP
httpx>=0.27.0,<1.0.0
requests>=2.32.3,<3.0.0
tenacity>=8.3.0,<9.0.0
feedparser>=6.0.11,<7.0.0
beautifulsoup4>=4.12.3,<5.0.0
lxml>=5.2.2,<6.0.0

# Data
pandas>=2.2.2,<3.0.0
numpy>=1.26.4,<3.0.0
pyarrow>=15.0.2,<17.0.0

# Persistence
SQLAlchemy>=2.0.30,<3.0.0

# Lightweight ML (CI doesn't need xgboost / prophet / bertopic)
scikit-learn>=1.5.1,<2.0.0
statsmodels>=0.14.2,<1.0.0

# NLP (spaCy + NLTK; BERTopic / transformers skipped)
spacy>=3.7.5,<4.0.0
nltk>=3.8.1,<4.0.0

# Dashboard + viz
streamlit>=1.36.0,<2.0.0
streamlit-extras>=0.4.0,<1.0.0
plotly>=5.22.0,<6.0.0
folium>=0.16.0,<1.0.0
streamlit-folium>=0.20.0,<1.0.0
pydeck>=0.9.1,<1.0.0
matplotlib>=3.9.0,<4.0.0
seaborn>=0.13.2,<1.0.0
wordcloud>=1.9.2,<2.0.0
pystache>=0.6.0,<1.0.0
pycountry>=24.6.1,<25.0.0

# --- dev / test only ---
pytest>=8.2.2,<9.0.0
pytest-cov>=5.0.0,<6.0.0
pytest-mock>=3.14.0,<4.0.0
pytest-asyncio>=0.23.7,<1.0.0

black>=24.4.2,<25.0.0
isort>=5.13.2,<6.0.0
ruff>=0.5.4,<1.0.0
bandit[toml]>=1.7.9,<2.0.0

vcrpy>=6.0.1,<7.0.0
responses>=0.25.0,<1.0.0
freezegun>=1.5.1,<2.0.0
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install system dependencies (for lxml)
        run: |
          sudo apt-get update
          sudo apt-get install -y libxml2-dev libxslt1-dev

      - name: Install runtime + lint deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-ci.txt
          pip install black==24.4.2 isort==5.13.2 ruff==0.5.4 bandit==1.7.9

      - name: ruff
        run: ruff check src tests

      - name: black --check
        run: black --check src tests

      - name: isort --check
        run: isort --check-only src tests

      - name: bandit
        run: bandit -r src -c pyproject.toml --quiet

  test:
    name: Test (${{ matrix.os }}, ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install system dependencies (Ubuntu only, for lxml)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxml2-dev libxslt1-dev

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-ci.txt

      - name: Download spaCy English model
        run: |
          python -m spacy download en_core_web_sm || echo "spaCy model not available, skipping"

      - name: Run tests
        env:
          NEWS_API_KEY: dummy-news-key-for-ci
          APP_ENV: ci
          LOG_LEVEL: WARNING
        run: pytest -m "not integration and not e2e and not slow" --tb=short -q

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  security:
    name: Security scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install detect-secrets
        run: pip install detect-secrets==1.5.0

      - name: Scan for secrets
        run: |
          if [ ! -f .secrets.baseline ]; then
            detect-secrets scan --exclude-files '\.lock$' > .secrets.baseline
          fi
          detect-secrets scan --exclude-files '\.lock$' --baseline .secrets.baseline

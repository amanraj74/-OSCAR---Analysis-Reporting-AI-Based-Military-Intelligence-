# Notebooks

Drop exploratory notebooks here (EDA, model interpretation, ad-hoc analysis).

Conventions:

- Filename prefix: `NN_short_slug.ipynb` (e.g. `01_gdelt_exploration.ipynb`)
- Use `sys.path.insert(0, '..')` to import from `src/`
- Set the working directory to the repo root before running.